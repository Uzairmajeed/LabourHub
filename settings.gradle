pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // add the following
        maven { url "https://jitpack.io" }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // add the following
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "LabourHub"
include ':app'
